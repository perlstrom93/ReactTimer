var React = require('react');

var Timer = React.createClass({
    render: function(){
        return(
            <div>
                Timer.js
            </div>
        );
    }
});

module.exports = Timer;
